[package]
name = "bpf-mcp-example"
version = "0.1.0"
edition = "2024"

[dependencies]
libbpf-rs = "0.26"
nix = { version = "0.29", features = ["poll"] }
anyhow = "1"
serde_json = "1"

[build-dependencies]
libbpf-cargo = "0.26"
